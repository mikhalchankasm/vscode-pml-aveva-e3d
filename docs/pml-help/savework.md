# SAVEWORK Safety

Agent Kit code: `PML_UNSAFE_SAVEWORK_IN_LOOP`

This finding means `SAVEWORK` appears in a loop or repeated execution path. Review transaction boundaries before running the macro because repeated saves can be slow, hard to recover, or unsafe in shared projects.
